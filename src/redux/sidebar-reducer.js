let initialState = {
    friends: [
        { id: 1, name: 'Margo' },
        { id: 2, name: 'Aisara' },
        { id: 3, name: 'Karro' }
    ]
};

const sidebarReducer = (state=initialState, action) => {

    return state;
}

export default sidebarReducer;